public  struct GlobalEnums
{
    public enum Options { RotateClockWise, RotateCounterClockwise, Destroy, DestroyWithColors, Rotate3x3Right };
    public enum SelectionMode { ThreeByThree, Single };
}
